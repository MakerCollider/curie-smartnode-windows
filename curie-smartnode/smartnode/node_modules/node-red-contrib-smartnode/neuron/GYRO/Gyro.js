module.exports = function init(RED) {
    function GyroNode(n) {
        RED.nodes.createNode(this, n);
        this.name = n.name;
        var node = this;

        this.on('input', function (msg){

        });
        this.on('close', function() {
        });
    }
    
    RED.nodes.registerType("Gyro", GyroNode);
};
