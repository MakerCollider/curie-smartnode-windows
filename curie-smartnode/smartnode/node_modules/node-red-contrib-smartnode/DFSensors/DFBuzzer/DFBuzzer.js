module.exports = function init(RED) {
    function DFBuzzerNode(n) {
        RED.nodes.createNode(this, n);
        this.name = n.name;
        this.digitalPin = n.digitalPin;
        var node = this;

        this.on('input', function (msg){

        });
        this.on('close', function() {
        });
    }
    
    RED.nodes.registerType("DFBuzzer", DFBuzzerNode);
};
